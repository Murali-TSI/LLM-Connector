[project]
name = "llm-connector"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=2.15.0",
    "pydantic>=2.12.5",
]

[dependency-groups]
dev = [
    "python-dotenv>=1.2.1",
]
